export const metadata = {
    title: "Aktiviter",
    description: "Find og tilmeld danse aktiviteter"
}

export default function SearchLayout ({ children }) {
	return (
		<>
        {children}
		</>
	)
}